import type { CatalogComponents } from "./types.js";

export const ToggleButtonGroup: CatalogComponents["ToggleButtonGroup"] = ({
  children,
}) => <div className="hap-toggle-button-group">{children}</div>;
