import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as stories from "./menu.stories.ts";

<Meta of={stories} />

# Menu

Menus show a list of menu items on a temporary surface. They appear when the
user interacts with a button, an action or another control element.

<Canvas of={stories.Default} />
