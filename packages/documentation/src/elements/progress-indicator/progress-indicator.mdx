import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as ProgressIndicatorStories from "./progress-indicator.stories.ts";

<Meta of={ProgressIndicatorStories} />

# Progress Indicator

Progress Indicators inform the user about the state of running processes, e.g.
loading an app, submitting a for or saving updated data.

<Canvas of={ProgressIndicatorStories.Default} />
