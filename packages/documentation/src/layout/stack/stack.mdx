import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as stories from "./stack.stories.ts";

<Meta of={stories} />

# Stack

A `stack` layouts its inner content vertically.

<Canvas of={stories.Default} />
